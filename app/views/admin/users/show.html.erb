<div class="max-w-3xl mx-auto bg-white shadow-lg rounded-2xl p-6">
  <h1 class="text-2xl font-bold text-gray-800 mb-4">Detail klienta</h1>

  <div class="bg-gray-100 p-4 rounded-lg mb-6">
    <p class="text-lg"><strong>Email:</strong> <%= @client.email %></p>
    <p class="text-lg"><strong>Meno:</strong> <%= @client.name %></p>
  </div>

  <h2 class="text-xl font-semibold text-gray-700 mb-3">Priradené plány</h2>

  <% if @client.plans.any? %>
    <ul class="space-y-2">
      <% @client.plans.each do |plan| %>
        <li class="bg-blue-100 text-blue-800 p-3 rounded-lg shadow-sm flex justify-between items-center">
          <div class="flex-1">
            <strong><%= plan.name %></strong> - <%= plan.description.truncate(30) %>
          </div>
          <div>
            <%= link_to "Odstrániť plán", remove_plan_from_client_admin_plan_path(plan, user_id: @client.id),
                data: { turbo_method: :delete, turbo_confirm: "Naozaj chceš odstrániť tento plán?" }, 
                class: "px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 text-sm" %>
          </div>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="text-gray-500 italic">Klient nemá priradené žiadne plány.</p>
  <% end %>

  <div class="mt-6">
    <%= link_to "Späť na zoznam klientov", admin_users_path, class: "bg-gray-200 hover:bg-gray-900 text-black px-4 py-2 rounded-lg shadow-md transition" %>
  </div>
</div>
